import Hero from "@/components/Hero";

/* The home page is the hero: the preloader draws Pakistan, hands the dot
   field to the map, and the headline arrives around it. Everything below
   the fold on this site is a page of its own, reached from the nav. */
export default function Home() {
  return <Hero />;
}
